require 'spec_helper'

describe SessionsController do

  describe "a new athlete is created" do
    it "creates a new athlete and session" do
      # get 'new'
      # expect(response).to be_success
    end
  end

  describe "a returning athlete signs in" do
    it "logs the athlete in" do
      # get 'create'
      # expect(response).to be_success
    end
  end

  describe "an athlete signs out" do
    it "destroys the session and takes you to sign in page" do
      # get 'destroy'
      # expect(response).to be_success
    end
  end

end
